import 'package:flutter_test/flutter_test.dart';

import 'package:dogmanagement/main.dart';

void main() {
  test('DogManagementApp can be created', () {
    const app = DogManagementApp();
    expect(app, isA<DogManagementApp>());
  });
}
